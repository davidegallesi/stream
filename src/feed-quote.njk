---
permalink: /feed/quote.xml
eleventyExcludeFromCollections: true
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Stream — quote</title>
  <subtitle>Solo post di tipo quote.</subtitle>
  <link href="{{ site.siteUrl }}/feed/quote.xml" rel="self"/>
  <link href="{{ site.siteUrl }}/"/>
  <id>{{ site.siteUrl }}/feed/quote/</id>
  {% if collections.stream_quote.length %}
  <updated>{{ collections.stream_quote[0].date | rfc3339 }}</updated>
  {% endif %}
  {% for post in collections.stream_quote | head(20) %}
  <entry>
    <title>quote — {{ post.date | htmlDateString }}</title>
    <link href="{{ site.siteUrl }}{{ post.url }}"/>
    <id>{{ site.siteUrl }}{{ post.url }}</id>
    <updated>{{ post.date | rfc3339 }}</updated>
    <content type="html"><![CDATA[{{ post.templateContent | safe }}]]></content>
  </entry>
  {% endfor %}
</feed>
